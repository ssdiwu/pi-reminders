# pi-reminders 术语表

## 命令路由

`/reminders` extension 对空参和非空参数作出的入口分流：空参直接列出待办，非空文本交回当前 Pi session。

## 意图理解

LLM 将用户的非空自然语言映射为 `reminders` tool 的 `action` 与结构化参数的过程。不属于 extension 的职责。

## Tool 执行

`reminders` tool 根据已结构化的 `action` 调用 AppleScript 操作 Reminders.app 的过程。

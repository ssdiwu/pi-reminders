# 0003 — 工具结果人类可读投影

- 状态：已采纳
- 日期：2026-07-16

## 决策

`reminders` 工具的结果默认显示紧凑的人类可读摘要；通过当前配置的工具展开快捷键（默认 `Ctrl+O`）展开时显示完整的操作文本。Apple Reminders ID、计数等 `details` 仅供模型与运行时使用，不由宿主默认渲染，也不以 JSON 展示。

## 后果

显示层不改变 add/list/complete/delete/update 的既有 `content` 和 `details` 契约。回归测试须覆盖折叠、展开和结构化详情不直出。

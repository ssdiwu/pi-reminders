# 路线图

## 当前版本

`v0.5.0` 已发布：在 v0.4 极简路由基础上新增列表阅读（`dueFrom` / `dueTo` 闭区间、到期日升序、`limit`）与批量删除一次确认（`queries[]`、逐目标解析去重、一次确认、尽力删除逐条结果）。实施方案见 [`../40-版本实施方案/v0.5.0-列表阅读与批量删除确认.md`](../40-版本实施方案/v0.5.0-列表阅读与批量删除确认.md)。

历史：

- `v0.4.0`：`/reminders` 收敛为空参确定性列表 / 非空交回当前 Pi session 的极简路由；旧别名与手写动词解析移除。

## 后续候选

- 多清单管理：仅在现有默认清单模型确实不足时再规划。
- 安装与 marketplace（市场）发布体验：在能力边界稳定后完善。
